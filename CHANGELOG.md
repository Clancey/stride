# Changelog

Every released version of Stride, newest first.

Generated by `tools/changelog.sh regenerate` from the tags in this repository, so it is a record of
what shipped rather than what was intended. Notes for a version come from
`docs/release-notes/<version>.md` when that file exists, and from the commit subjects since the previous
tag when it does not.

## 1.0.11 — versionCode 12 — 2026-08-15

- Call Connect, and don't claim a workout the treadmill hasn't started ([#8](https://github.com/Clancey/stride/pull/8))

## 1.0.8 — versionCode 9 — 2026-08-14

- Make Start start a workout, never resume the console's old one ([#7](https://github.com/Clancey/stride/pull/7))

## 1.0.7 — versionCode 8 — 2026-08-14

**Remembers the last catalog across a restart.** The last-check timestamp was persisted but the catalog was not, so a launcher restart inside the 30 minute check guard suppressed the check *and* left the store empty — "not checked yet", no header badge, while an update was genuinely pending. It stayed hidden until you tapped **Check now** or half an hour passed. The badge is the only thing that tells you an update exists, and this launcher restarts often, so that was worth fixing properly rather than treating as cosmetic.

The catalog is now cached after it parses (never before — one bad deploy would otherwise poison every subsequent start), and the plan is recomputed on restore, because what's installed changes between processes.

**Release automation.** The `v1.0.6` tag run failed with an empty signer digest that read like a signing failure and wasn't one. Fixed, along with the real cause underneath it: build-tools 37 prints a different certificate format than 36. Tagging a release that was already published now exits green instead of red.

Installs as an update over 1.0.6 — the notification-listener grant survives an update, unlike a reinstall.

Tested on a NordicTrack Commercial 1750.

## 1.0.6 — versionCode 7 — 2026-08-14

Install prompts now appear. This firmware was silently dropping the `PackageInstaller` status broadcast to a manifest-declared receiver, so every install hung forever with no dialog and no error — the receiver is now registered at runtime and the status intent is action-addressed.

Also in this release:
- checks for updates on startup (gated at 30 minutes), not just on the ~6h schedule
- the header badges when Google Play is available to install, not only for updates to installed apps
- store rows show real app icons

The APK is served from the [catalog](https://github.com/Clancey/stride-catalog/releases/tag/stride-7) so consoles can fetch it without auth. Consoles running 1.0.5 will offer this as a prompted update.
